import 'package:flutter/material.dart';

class FindWayScreen extends StatelessWidget {
  const FindWayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Dò đường"),),
      body: SafeArea(child: Stack(
        children: [
          
        ]
      )),
    );
  }
}
